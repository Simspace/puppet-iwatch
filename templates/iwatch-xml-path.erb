<%-
line = '    <path'
if @recurse == 'enabled'
  line << ' type="recursive"'
else
  line << ' type="single"'
end
if @syslog == 'enabled'
  line << ' syslog="on"'
else
  line << ' syslog="off"'
end
line << '>' + @path + '</path>'
-%>
<%= @line %>
